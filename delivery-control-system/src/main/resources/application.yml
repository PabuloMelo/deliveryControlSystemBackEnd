spring:
  datasource:
    url: jdbc:h2:mem:delivery_control_system
    driverClassName: org.h2.Driver
    username: Pabulo
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-alow-others: false